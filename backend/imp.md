##Very Important

We need to temporarily serve our backend from this device.
We need to expose port 22, 80, 8000. For SSH, HTTP and FastAPI.
Our actual complete project is hosted.
The frontend is hosted on Vercel, while the microservices are setup on AWS.
We just need to use this PC as the main API backend for now.